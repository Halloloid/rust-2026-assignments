# Solution notes: Character frequency, sorted

## Approach

_stored the values in map used the hint used and_modify helper method so if vallue exits then in closure i take the value and inc by 1 after that i store it in a Vec and then done sort_by added a closure in that first checks for frequency if same then comapre charcters using ASCII_

## Edge cases handled
_early returning in the string is empty_

## Anything special

_Tricks i had used helper method and closures_
