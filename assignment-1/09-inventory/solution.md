# Solution notes: Inventory

## Approach

_for the restock i converted the inventory into hashmap and traversed the more and used the helper functions of hashmap to add and store the units if names are same else add the new element than converted that Hashmap to Vec and returend and for Summary it was very Straight forward_

## Edge cases handled
_if values exists than add the qunatities_

## Anything special

_used iterator instead of loops as i need to writ less_
